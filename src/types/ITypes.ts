export interface ITypes {
  num: string;
  name: string;
  count: string;
  year: string;
  shape: string;
  color: string;
  size: string;
  favorite: boolean;
}

export interface ISliderValue {
  min: number,
  max: number
}

export interface IOptions {
  title: string,
  value: string,
  id: string
  type: string
}